import { styled } from 'styled-components';

const CareerInfoWrapper = styled.div`
  .career-row {
    align-items: baseline;
    display: flex;
    gap: 8px;
  }

  .career-row-title {
    text-transform: capitalize;
  }
`;

export default CareerInfoWrapper;
