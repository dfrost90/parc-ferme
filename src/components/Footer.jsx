import { FooterWrapper } from './wrappers';

const Footer = () => {
  return (
    <FooterWrapper>
      Created by dfr0st (c) {new Date().getFullYear()}
    </FooterWrapper>
  );
};

export default Footer;
